
def download(RA,DEC):
    error = 0
    # Check for failure, increment error
    return error

def download_LRG():
    print("Progress")
    errors = 0
    # Parse LRG file
    # For each item
    # Call download
    # error = download(RA,DEC)
    # errors = errors + error
    return errors

def download_unLRG():
    print("Progress")
    errors = 0
    # Parse LRG file
    # For each item
    # Call download
    # error = download(RA,DEC)
    # errors = errors + error
    return errors